# Copy to .env.local (gitignored) if you need to point at a non-default backend.
# Defaults to http://127.0.0.1:8000 if unset — matches the backend's default port.
VITE_API_BASE_URL=http://127.0.0.1:8000
